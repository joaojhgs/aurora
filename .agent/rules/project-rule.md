---
trigger: always_on
---

Always use "source .venv/bin/activate" (or "uv run <command>") before running any commands or parts of the software to ensure the correct environment is loaded with all dependencies. Do NOT use Conda.
