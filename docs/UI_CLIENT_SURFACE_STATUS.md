# Aurora UI client-surface status

**Status:** Current bounded check

**Snapshot date:** 2026-07-30

**Audience:** contributors deciding what Aurora UI can claim today

This document records the current repository, tracked reports, maintained commands, and workflow-owned checks behind the client-surface roadmap. It is a bounded source/build/harness snapshot, not a signed-release, live production deployment, app-store approval, or broad physical-device certification. Refresh this page when implementation, commands, workflows, or checked reports change.

## Current integration boundary

- Hosted browser real Python-peer E2E is passing for the current runtime-role implementation.
- Desktop client source, bundle validation, native assistant-provider composition, and Linux native WebRTC fallback are implemented. The packaged Python-free Linux desktop live E2E is maintained by `pnpm test:desktop-client:live` and passed on current main HEAD; packaged macOS and Windows live behavior remains external platform evidence.
- Android static, build, generated-project, preflight, APK, and AAB artifact gates pass on current main HEAD. Android emulator/device WebRTC smoke remains externally blocked in this workspace because adb and Mobile MCP have no device, the x86_64 AVD cannot use `/dev/kvm`, ARM64 software emulation is unsupported on x86_64 QEMU2, and no Waydroid/binder route is available. Treat Android runtime validation as pending usable KVM access or an authorized physical device.
- iOS policy/source/frontend/overlay checks are implemented and Linux-safe. iOS simulator, MobileSafari, packaged WKWebView, Swift runtime smoke, signing, and App Store evidence require macOS/Xcode with the needed simulator/runtime/toolchain prerequisites and remain external.
- Release signing, notarization, Play/App Store distribution, production TURN scale, and physical-device certification are outside this local snapshot.

For target clients and sequencing, read [`UI_CLIENT_SURFACE_ROADMAP.md`](UI_CLIENT_SURFACE_ROADMAP.md). For the live interop harness, read [`WEBRTC_LIVE_INTEROP_HARNESS.md`](WEBRTC_LIVE_INTEROP_HARNESS.md). For the WebView protocol contract, read [`WEBVIEW_WEBRTC_PROTOCOL_CONTRACT.md`](WEBVIEW_WEBRTC_PROTOCOL_CONTRACT.md).

## Executive state

- The shared TypeScript/WebView runtime is implemented for hosted web, desktop Tauri client, Android client, and the iOS client source path. It supports `http-only`, `webrtc-only`, and `webrtc-preferred` connection modes, with runtime profiles selecting remote-console or mesh-node behavior independently of physical surface. On Linux desktop client, the runtime injects a narrow Rust `RTCPeerConnection`/`RTCDataChannel` primitive only when system WebKitGTK omits the browser API; signaling, cryptography, pairing, reconnect, authorization, and RPC remain shared TypeScript. Packaged Python-free clients compile no operator endpoint into the frontend; first-run onboarding accepts only a local node name, runtime role, and QR/file/deep-link/pasted mesh invite, while advanced runtime HTTP/HTTPS Gateway and WS/WSS signaling profile editing remains in normal connection settings.
- Live browser-to-Python Gateway WebRTC interop passes with the Python HTTP API disabled. Direct, configured-STUN, and forced-TURN foreground sessions pass in Chromium, Firefox, and Playwright WebKit. Every lane is backed by the browser `RTCPeerConnection.getStats()` selected candidate pair. Signaling is MQTT only; application RPC/events use the `aurora-rpc` `RTCDataChannel`.
- The tracked live lanes cover deterministic offer/answer roles in both directions, manifest exchange, structured error parity, bidirectionally fragmented 512 KiB RPC, stream completion and Python-observed cancellation, bilateral SAS pairing, reconnect validation without re-entering SAS, revocation fail-closed, scoped event/correlation isolation, redacted secret scanning, and an uncertain-loss mutation window: `G009Interop.Mutate` starts, `G009Interop.MutationStarted` is observed, the browser disconnects before the response settles, and Python records `execution_count=1`. The reports are under `reports/webrtc-interop/{direct,firefox-direct,webkit-direct,stun,firefox-stun,webkit-stun,turn,firefox-turn,webkit-turn}/report.json`.
- Recoverable WebRTC epoch changes reject pending calls, subscription acknowledgements, manifest requests, and streams as transport loss; they do not silently clear timeouts or replay in-flight calls. Native reconnect storage reports “not found” separately from backend failure: only the former may proceed to SAS pairing, while backend failure produces a typed redacted diagnostic and fails closed.
- Desktop-client AppImage/deb artifact validation, packaged Linux desktop live validation, and Android client debug APK/AAB artifact validation pass. Legacy `*thin*` scripts delegate to the neutral client commands. Python-free client bundles contain no Python executable, Python runtime assets, `config_defaults.json`, `site-packages`, or sidecar resources. Desktop-local remains a separate Rust-supervised Python sidecar path.
- `pnpm test:desktop-client:live` validates runtime invite import, bilateral matching SAS, non-admin scoped principal authorization, native Rust WebRTC fallback with the Python HTTP API disabled, role switching with distinct sessions, restart/reconnect, revocation fail-closed, no Python child process or sidecar, and zero forbidden compiled endpoints or secrets for the packaged Linux desktop client. It is Linux packaged-WebView coverage; it does not validate packaged macOS or Windows WebView networking.
- Android foreground WebView microphone policy, lifecycle release policy, Keystore-backed peer credential validation commands, generated-project sync, client bundle tests, CI preflight, and APK/AAB artifact scans are implemented and build-proven. Python, TypeScript, Rust, Kotlin, and Swift use the same canonical `mesh_auth_proof_v1` HMAC transcript, including Python-compatible ASCII escaping for Unicode peer/room values. Current local Android runtime E2E is not passed in this workspace because no usable emulator/device route is available. Do not claim Android Python-peer WebRTC, physical-device, release-signing, Play distribution, or durable background wakeword evidence from this snapshot.
- Playwright Chromium, Firefox, and WebKit are installed in the current verification environment and all three direct/STUN/TURN report sets pass. The hosted Chromium peer product flow is maintained by `pnpm test:hosted-peer:live`; hosted mesh-node coverage is maintained by `scripts/hosted_mesh_node_e2e.sh`; browser encrypted persistence is maintained by `pnpm test:web-persistence`. Those commands/scripts write local generated diagnostics rather than committed docs. The browser direct/STUN/TURN matrix passed all 9 Chromium/Firefox/Playwright-WebKit cases without skips. The existing API 35 Android workflow runs the same protocol assertions twice against an external Python peer: once in the packaged System WebView and once in standalone Android Chrome without CDP. The existing macOS iOS workflow runs the same direct-path assertions twice on an iPhone simulator: once in MobileSafari and once in a dedicated packaged Tauri WKWebView app that is scanned for forbidden Python/sidecar paths. Passing mobile runtime validation depends on usable Android KVM/device access and macOS/Xcode iOS runners. Page suspension, physical/OEM paths, and production browser certification remain unclaimed.
- Ordered DataChannel delivery is preserved through asynchronous frame decryption. A regression test delays the first encrypted frame and verifies a pairing reveal cannot overtake its preceding commitment; the previously flaky Firefox TURN lane then passed three consecutive runs before the full cross-engine matrix passed.
- Android CI now installs compile SDK 36 (while retaining the API 35 emulator image) and all four Android Rust targets required by the universal AAB. Current-main Android gates include 14 passing client bundle tests, `android:init` generated-project sync, CI preflight at `2026-07-30T14:47:59.342Z` with 27 required checks passed and only optional signing blocked, an x86_64 debug APK whose artifact scan found no Python/sidecar/endpoint/secrets with SHA-256 `e3e8d78a352bfc925f4bcfd70852705a80ffd3e437dccdc657fd6cd8ee92e0c6`, and a universal four-ABI debug AAB of 237,597,038 bytes with SHA-256 `a042e1ec1a84e11636ea0e05ca1d207c610d0080df3ffbe96117e0b00df5564a`, 1,016 entries inspected, all expected capabilities present, zero forbidden matches, and secrets redacted.
- iOS client source wiring is implemented: the Tauri runtime selects the shared WebView HTTP/WebRTC path, the Rust commands route to Swift, peer reconnect bearer material and room secrets are held in device-only/non-synchronizing Keychain items, and nonsecret profiles use `UserDefaults`. Current-main Linux-safe gates `ios:policy`, `build:frontend:ios-client`, and `ios:prepare:client` pass, and the consolidated SDK/UI tests and builds also pass. This environment cannot produce Xcode artifacts or run iOS simulators, so iOS remains simulator/runtime-limited until macOS/Xcode simulator/device direct-STUN-TURN, packaged WKWebView, Swift runtime smoke, microphone/lifecycle, signing, and App Store/TestFlight evidence passes.
- Python-in-Rust compilation or embedding was rejected/deferred. Python-free client shells do not need Python; desktop-local keeps Python as a separate supervised sidecar process.
- Hosted web now persists the reconnect bearer and room secret only as AES-GCM ciphertext in IndexedDB, persists a validated nonsecret runtime profile and stable browser peer ID in `localStorage`, scrubs the invite fragment, and automatically re-dials the saved profile after a refresh. If IndexedDB, WebCrypto, key structured-cloning, or metadata storage fails, the runtime reports the downgrade and remains memory-only; it never writes plaintext secrets as a fallback.
- Browser clear-device-data now has a scoped lifecycle: it removes Aurora peer vault/profile metadata, the local-data backend pointer, the storage lease, the envelope-key database, the local-data IndexedDB database, and the OPFS node directory for the local node, while preserving unrelated same-origin data. Cleanup reports structured per-step results; any partial failure remains a failure and must not be presented as successful device clearing.
- The hosted product-flow gate `pnpm test:hosted-peer:live` now passes in Chromium against an isolated full Python Auth/DB/Gateway/WebRTC service; `pnpm test:web-thin:live` remains a compatibility alias. It validates invite-first onboarding, matching bilateral SAS, both approvals with a scoped non-admin principal, authenticated DataChannel diagnostics, persisted bidirectional peer approval, scoped route discovery including fragmented catalog/registry responses, Mesh refresh and SPA navigation, zero browser Gateway HTTP requests, no blur-event disconnect, non-extractable AES-GCM IndexedDB envelopes, and reload reconnection without pairing again. The Playwright test process uses the temporary Gateway API only for server-side setup/approval/diagnostics; that control path is not available to the browser. Actual OS tab/page suspension remains unproven.
- Reversible WebRTC rollout gates are implemented across hosted web and Tauri/WebView shells. Disabling the main legacy client gate sends only `webrtc-preferred` profiles with an explicit Gateway to HTTP, keeps `webrtc-only` fail-closed, leaves desktop-local intact, and neither loads nor rewrites saved peer credentials. Scoped-subscription and fragmentation/backpressure gates alter the local capability hello, so the Python and TypeScript peers use the same negotiated intersection. An E2EE-required profile fails closed when its local E2EE gate is disabled. The Gateway legacy non-sensitive broadcast compatibility path is separately configurable and hot-reloadable; sensitive/scoped-only topics remain blocked.

## Current client readiness

Legend: **Implemented** means a usable code path exists within the stated boundary; **Build-proven** means artifact/package checks ran successfully; **Live-proven** means a live interop or runtime harness passed; **Planned/limited** means important platform validation or implementation is still missing.

| Client profile | Current state | Evidence and boundary | Main gaps before broader support claim |
| --- | --- | --- | --- |
| Hosted web remote console over HTTP | **Implemented, bounded** | `apps/aurora-web/app/aurora-client.ts` selects HTTP transport from the saved runtime profile and gates first launch through onboarding when no profile exists. | Deployment-specific TLS/CORS/auth and operator endpoint rollout remain environment-specific. |
| Hosted web WebRTC remote console / mesh node | **Implemented, cross-engine protocol and hosted-Chromium UI live-proven** | `createBrowserWebThinRuntime()` and `createBrowserWebRtcAuroraRuntime()` support `webrtc-only`/`webrtc-preferred`; runtime profiles carry remote-console or mesh-node role state. Chromium/Firefox/Playwright-WebKit direct, configured-STUN, and forced-TURN tracked reports passed with the HTTP API disabled. `pnpm test:hosted-peer:live` validates the hosted peer product flow against the full Python service with real onboarding/SAS/bilateral approval/scoped routes/Mesh/SPA blur/reload behavior and zero browser Gateway HTTP requests. `scripts/hosted_mesh_node_e2e.sh` validates the hosted mesh-node product flow. `pnpm test:web-persistence` validates browser encrypted persistence. These hosted commands/scripts write local generated diagnostics rather than committed docs. The hosted shell uses an encrypted IndexedDB credential/room vault, validated nonsecret profile metadata, a stable browser peer identity, and refresh re-dial. | Android/iOS native-WebView runtime results, production signaling/TURN scale, long-running reconnect/load, actual page suspension, public hosted deployment evidence, and WebAuthn-bound vault unlock remain unproven. |
| Desktop Tauri local + Python sidecar | **Implemented, bounded** | Tauri local mode supervises an external Python thread-mode sidecar, probes loopback Gateway, and writes `apps/aurora-tauri/reports/tauri-dev-smoke.json`. | Signed/notarized release artifacts and full profile/hardware matrices remain release-specific. |
| Desktop Tauri client over HTTP/WebRTC | **Implemented, build-proven; Linux packaged live-proven** | Async nonsecret runtime profile load/edit/save/select reconstructs the shared runtime for `http-only`, `webrtc-only`, or `webrtc-preferred` remote-console/mesh-node use. First-run onboarding accepts a node name plus invite and derives signaling/pairing material; advanced endpoint/profile controls remain in normal settings. Desktop keychain stores room secrets separately from the profile. Desktop-client package validation uses general connect policy and passes with no forbidden Python/sidecar matches. Linux builds include a target-only `webrtc-rs` peer primitive and a least-privilege Tauri capability for distributions whose WebKitGTK lacks `RTCPeerConnection`; macOS/Windows continue to use the WebView API. `pnpm test:desktop-client:live` validates runtime invite import, bilateral matching SAS, scoped non-admin authorization, native Rust WebRTC fallback with Python HTTP API disabled, distinct role-switch sessions, restart/reconnect, revocation fail-closed, no Python child/sidecar, and zero forbidden compiled endpoints/secrets for the packaged Linux client. Native assistant-provider composition is wired through Rust/native status and catalog calls without exposing provider secrets to JavaScript. | Packaged macOS/Windows WebView network evidence remains platform-runner evidence. Signing/notarization and broader desktop hardware/network matrices remain release-specific. |
| Android client over HTTP/WebRTC | **Implemented, build/artifact-proven; device runtime blocked** | Android client overlay, native plugin sync, Keystore peer credential/proof and room-secret commands, WebView microphone policy, lifecycle status, 14 passing Android client bundle tests, `android:init` generated-project sync, CI preflight with 27 required checks passed, and x86_64 debug APK/universal four-ABI debug AAB artifact scans pass. The APK SHA-256 is `e3e8d78a352bfc925f4bcfd70852705a80ffd3e437dccdc657fd6cd8ee92e0c6`; the AAB SHA-256 is `a042e1ec1a84e11636ea0e05ca1d207c610d0080df3ffbe96117e0b00df5564a` with 1,016 entries inspected. The API 35 workflow owns packaged-System-WebView and standalone-Android-Chrome forced-TURN pairing/RPC/reconnect/revocation against an external Python peer. | This workspace cannot produce a passing Android emulator/device runtime E2E because adb and Mobile MCP have no device, the x86_64 AVD cannot use `/dev/kvm`, ARM64 software emulation is unsupported on x86_64 QEMU2, and Waydroid/binder is unavailable. Android runtime evidence requires usable KVM access or an authorized physical device. Release signing, Play/App Distribution, physical STUN/TURN WebRTC paths, and durable background voice remain unproven. |
| Android native-enhanced | **Partial/limited** | Native plugin reports assistant role, fallback entrypoints, notifications, biometric/admin unlock, foreground service readiness, and media policy status. | Foreground service coverage is permission/lifecycle readiness, not a completed durable wakeword/audio/model pipeline. Durable background wakeword is not claimed. |
| iOS client over HTTP/WebRTC | **Implemented in source; Linux source/policy/frontend/overlay-proven** | `apps/aurora-tauri/src/aurora-client.ts` routes iOS through the shared WebView client runtime and native opaque credential store. Rust routes the profile, peer-credential, and room-secret commands to Swift. `AuroraThinPeerStorage.swift` uses hashed accounts, `kSecAttrAccessibleWhenUnlockedThisDeviceOnly`, no Keychain sync/raw getter for reconnect bearer material, canonical HMAC reconnect validation, device-only room secrets, and nonsecret `UserDefaults` profiles. `ios:policy`, `build:frontend:ios-client`, and `ios:prepare:client` pass on Linux for policy/source invariants and runtime-configurable client overlay generation with no external binaries/resources. Legacy iOS thin commands delegate to the client commands. | Linux cannot build or run iOS. Simulator/MobileSafari/packaged WKWebView, Swift runtime smoke, microphone/lifecycle behavior, physical-device direct/STUN/TURN smoke, signing, and TestFlight/App Store evidence remain pending and require macOS/Xcode plus simulator/runtime/toolchain prerequisites. |
| iOS native-enhanced | **Planned/limited** | Policy copy forbids default system-assistant claims and allows only Siri/Shortcuts/App Intents integration. | App Intents/share/widget targets must be wired and smoked on macOS/Xcode; durable background voice and raw audio are unsupported without explicit Apple-permitted evidence. |
| PyQt local fallback | **Maintained fallback** | Python UIBridge remains as legacy local/reference behavior. | Not the target for new multi-platform UI or WebView WebRTC behavior. |

## Implemented shared WebView runtime

`packages/aurora-ui/src/web-thin-runtime.ts` wraps `@aurora/client/webrtc` and constructs one surface-aware runtime for hosted web, desktop Tauri client, Android client, and iOS client surfaces. It exposes the same user-facing modes everywhere:

| Mode | Behavior |
| --- | --- |
| `http-only` | Uses HTTP Gateway transport and does not touch `RTCPeerConnection`, MQTT signaling, or peer credentials. |
| `webrtc-only` | Requires a WebRTC invite/profile and exact secure signaling origin, fails closed when no authorized peer is connected, and does not need or fall back to an Aurora HTTP Gateway. |
| `webrtc-preferred` | Uses WebRTC for authorized peer routes and allows HTTP only as an explicit fallback for new calls when a Gateway endpoint exists. It does not blindly replay uncertain mutations after transport loss. |

Runtime and UI evidence:

- `packages/aurora-sdk/src/webrtc/` implements browser/WebRTC crypto, MQTT signaling, peer sessions, RPC protocol parsing, flow control/backpressure, fragmentation, scoped subscriptions, manifests, pairing, reconnect proof, and `WebRtcMeshPeerBridge`.
- `packages/aurora-ui/src/web-thin-runtime.ts`, `packages/aurora-ui/src/browser-peer-persistence.ts`, and `packages/aurora-ui/src/web-thin-connection-panel.tsx` expose invite import, secure-context checks, persistence/fallback diagnostics, encrypted browser secrets, and validated profile metadata.
- `apps/aurora-web/app/aurora-client.ts` selects `http-only`, `webrtc-only`, or `webrtc-preferred` from the saved onboarding profile, restores the stable peer/profile, automatically re-dials after refresh, and keeps server-side demo fallback explicitly labeled.
- `apps/aurora-tauri/src/aurora-client.ts` and `apps/aurora-tauri/src/tauri-app.tsx` use the same runtime for desktop/mobile client profiles while keeping desktop-local sidecar behavior separate.
- `apps/aurora-tauri/src/native-webrtc.ts` and `apps/aurora-tauri/src-tauri/src/native_webrtc.rs` provide the Linux-only peer primitive when WebKitGTK has no `RTCPeerConnection`; they do not reimplement MQTT signaling, Aurora encryption/pairing, reconnect proof, or RPC.

Rollout inputs default to enabled for backward compatibility:

| Gate | Hosted web | Tauri/WebView | Verified rollback behavior |
| --- | --- | --- | --- |
| Legacy client entry point | `NEXT_PUBLIC_AURORA_WEBRTC_THIN_CLIENT` | `VITE_AURORA_WEBRTC_THIN_CLIENT` | `webrtc-preferred` uses configured HTTP; `webrtc-only` fails closed; stored peer credentials are not consumed, deleted, or migrated. |
| Scoped subscriptions | `NEXT_PUBLIC_AURORA_WEBRTC_SCOPED_SUBSCRIPTIONS` | `VITE_AURORA_WEBRTC_SCOPED_SUBSCRIPTIONS` | Removes `scoped_event_subscriptions_v1` from the local hello; both peers use the negotiated intersection. |
| Fragmentation/backpressure | `NEXT_PUBLIC_AURORA_WEBRTC_FRAGMENTATION` | `VITE_AURORA_WEBRTC_FRAGMENTATION` | Removes `fragmentation_v1` and `backpressure_v1`; oversized frames fail instead of using an unnegotiated extension. |
| Application E2EE allowance | `NEXT_PUBLIC_AURORA_WEBRTC_APP_LAYER_E2EE` | `VITE_AURORA_WEBRTC_APP_LAYER_E2EE` | A profile explicitly allowing DTLS-only JSON can run plaintext; a profile requiring app-layer E2EE fails closed. |

The Python compatibility gate is
`services.gateway.webrtc.legacy_event_broadcast`. It affects only the temporary
non-sensitive unscoped broadcast path and can be updated through normal
Gateway config reload.

## Live WebRTC interoperability validation

Required lanes pass through `scripts/webrtc_interop*.{sh,mjs,py}` and write `reports/webrtc-interop/<lane>/report.json`.
The WebRTC CI workflow runs the full browser/lane matrix with
`WEBRTC_INTEROP_REQUIRE_ALL_BROWSERS=1`, so unavailable Chromium, Firefox, or
WebKit runtimes fail the required lane rather than producing an accepted skip.

| Lane | Report/output | ICE path | Status | Key validation |
| --- | --- | --- | --- | --- |
| Chromium direct | `reports/webrtc-interop/direct/report.json` | `host` | Passed | The selected host/host pair comes from `getStats()` with no gathered STUN or relay evidence; registry read and events use `RTCDataChannel`; Gateway HTTP API is disabled/unreachable; no browser HTTP fetch transport calls. |
| Chromium STUN | `reports/webrtc-interop/stun/report.json` | `srflx` | Passed | The selected reflexive pair and configured-STUN candidate URL match come from `getStats()`. The same auth/event/mutation/revocation checks passed. |
| TURN | `reports/webrtc-interop/turn/report.json` | `relay` | Passed | `selectedCandidatePair.category=relay` from browser `RTCPeerConnection.getStats()`; relay-only policy forced TURN; same auth/event/mutation/revocation checks passed. |
| Firefox direct | `reports/webrtc-interop/firefox-direct/report.json` | `host` | Passed | Firefox completed the same HTTP-disabled DataChannel, auth, reconnect, mutation, scope, and redaction assertions. |
| Firefox STUN | `reports/webrtc-interop/firefox-stun/report.json` | `prflx` selected, configured `srflx` gathered | Passed | Firefox omitted the candidate URL from stats; exactly one configured STUN server makes the gathered source unambiguous, and the report records that evidence mode instead of fabricating a URL match. |
| Firefox TURN | `reports/webrtc-interop/firefox-turn/report.json` | `relay` | Passed | Firefox completed the same assertions under relay-only policy with a selected relay pair. |
| WebKit direct | `reports/webrtc-interop/webkit-direct/report.json` | `host` | Passed | WebKit completed the same assertions with a selected host/host pair from `getStats()` and no gathered STUN or relay evidence. |
| WebKit STUN | `reports/webrtc-interop/webkit-stun/report.json` | `srflx` | Passed | Playwright WebKit completed the same assertions with a selected configured-STUN reflexive pair from `getStats()`. |
| WebKit TURN | `reports/webrtc-interop/webkit-turn/report.json` | `relay` | Passed | Playwright WebKit completed the same assertions with a selected relay pair from `getStats()`. |
| Android System WebView TURN | Generated local output at `apps/aurora-tauri/reports/webrtc-interop/android-webview/report.json` | `relay` required | Implemented; external runtime validation blocked locally | `android-python-webrtc.e2e.test.ts` drives the packaged API 35 WebView over CDP, pairs with the same external Python peer, and requires the same DataChannel, reconnect, mutation, revocation, redaction, and no-HTTP-fallback assertions. This workspace needs usable KVM access or an authorized physical device before this can pass. |
| Android Chrome TURN relay | Generated local output at `apps/aurora-tauri/reports/webrtc-interop/android-mobile-browser/report.json` | relay required in CI | Implemented; external runtime validation blocked locally | `android-browser-python-webrtc.e2e.test.ts` launches standalone Android Chrome without CDP, auto-runs the shared browser harness, posts the result only after runtime close, and requires the same external-Python, DataChannel, reconnect, mutation, revocation, redaction, and no-HTTP-fallback assertions. CI forces TURN because emulator direct ICE host reachability is not stable enough for a required PR gate; this workspace needs usable KVM access or an authorized physical device before this can pass. |
| iOS MobileSafari simulator direct | Generated local output at `apps/aurora-tauri/reports/webrtc-interop/ios-mobile-safari/report.json` | direct required | Implemented; macOS/Xcode validation required | `ios-python-webrtc.e2e.test.ts` boots an iPhone simulator, launches MobileSafari without CDP/Web Inspector, starts a local WebSocket MQTT broker plus external Python peer, and requires the complete shared interop and redaction contract. Linux cannot run this gate. |
| iOS packaged Tauri WKWebView simulator direct | Generated local output at `apps/aurora-tauri/reports/webrtc-interop/ios-wkwebview/report.json` | direct required | Implemented; macOS/Xcode validation required | The same serial E2E builds and scans a dedicated unsigned Tauri WKWebView app, installs/launches it with `simctl`, and requires the same external-Python bilateral-pairing, DataChannel, reconnect, mutation, revocation, HTTP-disabled, and redaction assertions. Linux cannot build or run this gate. |

The passing reports assert:

- `gatewayApiEnabled=false`, `gatewayHttpReachable=false`, and `noHttpFetchTransportUsed=true`;
- `registryReadOverDataChannel=true`, `eventOverDataChannel=true`, and `ttsEventOverDataChannel=true`;
- `reconnectWithoutSas=true` and `authorizedWithoutSas=true` after canonical reconnect validation;
- `revokedCredentialFailsClosed=true` and `routeAuthorizedAfterRevocation=false`;
- `mutationAtMostOnce=true`, `mutationUncertainLossWindow=true`, `execution_count=1`, `startedAckBeforeDisconnect=true`, `responseSettledBeforeDisconnect=false`, and `disconnectBeforeResponseSettled=true`;
- `wildcardDelivered=false`, `wrongCorrelationDelivered=false`, and matching Python-side subscription isolation;
- `redaction.passed=true`, `secretsRedacted=true`, and no findings in the scanned lane reports.

## Packaging and platform evidence

| Artifact/profile | Command verified locally | Report/output | Current result |
| --- | --- | --- | --- |
| Desktop client AppImage/deb | `pnpm --filter @aurora/tauri-ui build:bundle:desktop-client` | Generated local output at `apps/aurora-tauri/reports/desktop-client-bundle-{prepare,proof}.json` | Passed; endpoint and role selection are runtime profile/onboarding state, the generated policy allows general HTTP/HTTPS/WS/WSS connect targets, and artifact scanning checks no forbidden Python/sidecar matches, no external binaries/resources, and the Python-free client capability set. Legacy desktop-thin commands delegate to this lane. |
| Desktop Linux packaged live E2E | `pnpm test:desktop-client:live` | Local generated diagnostics | Passed on current main HEAD; validates runtime invite and bilateral matching SAS, non-admin scoped principal, native Rust WebRTC fallback with Python HTTP API disabled, role switching with distinct sessions, restart/reconnect, revocation fail-closed, no Python child process or sidecar, and zero forbidden compiled endpoints/secrets. |
| Android client debug APK | `pnpm --filter @aurora/tauri-ui android:build:client:apk` then `android:verify:client:apk` | Generated local output at `apps/aurora-tauri/reports/android-client-apk-{build-provenance,artifact-proof}.json` | Passed for the x86_64 debug APK. Artifact scan found no Python/sidecar/endpoint/secrets; SHA-256 `e3e8d78a352bfc925f4bcfd70852705a80ffd3e437dccdc657fd6cd8ee92e0c6`. |
| Android client debug AAB | `pnpm --filter @aurora/tauri-ui android:build:client:aab` then `android:verify:client:aab` | Generated local output at `apps/aurora-tauri/reports/android-client-aab-{build-provenance,artifact-proof}.json` | Passed for the universal four-ABI debug AAB: 237,597,038 bytes, SHA-256 `a042e1ec1a84e11636ea0e05ca1d207c610d0080df3ffbe96117e0b00df5564a`, 1,016 entries inspected, all expected capabilities present, zero forbidden matches, and secrets redacted. |
| Android preflight | `pnpm --filter @aurora/tauri-ui android:preflight:ci` / `android:preflight` | Generated local output at `apps/aurora-tauri/reports/android-preflight.json` | Current-main preflight at `2026-07-30T14:47:59.342Z` passed 27 required generated-project/native-plugin checks; optional release signing is the only blocked item. |
| Android emulator/device runtime | `android:smoke`, `android:webrtc:interop`, or equivalent physical-device smoke | Generated local output under `apps/aurora-tauri/reports/webrtc-interop/android-*` | Blocked in this workspace by unavailable runtime access: adb and Mobile MCP have no device, the x86_64 AVD cannot use `/dev/kvm`, ARM64 software emulation is unsupported on x86_64 QEMU2, and no Waydroid/binder route is available. The code and workflow gates remain implemented; a passing runtime result requires usable KVM access or an authorized physical device. |
| Android API 35 packaged WebView ↔ Python peer | `pnpm --filter @aurora/tauri-ui android:webrtc:interop` | Generated local output under `apps/aurora-tauri/reports/webrtc-interop/android-webview/` | Implemented in the existing Android workflow; this workspace lacks usable KVM/device runtime access. Python is an external protocol peer only, and APK/AAB artifact validation remains Python-free. |
| Android API 35 standalone browser ↔ Python peer | `pnpm --filter @aurora/tauri-ui android:webrtc:mobile-browser` | Generated local output under `apps/aurora-tauri/reports/webrtc-interop/android-mobile-browser/` | Implemented without CDP and included in the same Android workflow/check as the packaged WebView test; this workspace lacks usable KVM/device runtime access. |
| iOS simulator MobileSafari ↔ Python peer | `pnpm --filter @aurora/tauri-ui ios:webrtc:interop` | Generated local output under `apps/aurora-tauri/reports/webrtc-interop/ios-mobile-safari/` | Implemented in the existing macOS iOS workflow with the shared browser assertions and scanner; passing validation requires macOS/Xcode. |
| iOS simulator packaged WKWebView ↔ Python peer | `pnpm --filter @aurora/tauri-ui ios:webrtc:interop` | Generated local output under `apps/aurora-tauri/reports/webrtc-interop/ios-wkwebview/` | Implemented in the same existing macOS job/check. The temporary Tauri test app declares no external binaries/resources and the built `.app` is scanned for forbidden Python/sidecar paths. Python and Mosquitto remain external test-only processes. |
| iOS client generated overlay | `pnpm --filter @aurora/tauri-ui ios:prepare:client` | Generated local output at `apps/aurora-tauri/reports/ios-client-bundle-prepare.json` | Passed with `ios:policy` and `build:frontend:ios-client` on Linux; generated general HTTP/HTTPS/WS/WSS connect policy, selected client/mobile mesh capabilities, and declared no external binaries or resources. This is source/config/frontend validation, not an Xcode artifact or iOS runtime validation. Legacy iOS thin commands delegate to this lane. |
| Desktop local smoke | `pnpm --filter @aurora/tauri-ui dev:smoke` | Generated local output at `apps/aurora-tauri/reports/tauri-dev-smoke.json` | Passed in the recorded output; validates loopback Gateway readiness and `[tauri]`/`[aurora][...]` logs for desktop-local, not client WebRTC. |

Client package validation is an artifact-content claim only. In this snapshot, Linux desktop packaged live E2E is validated, while packaged macOS/Windows live behavior remains external platform evidence. Android emulator/device runtime validation is blocked locally and must come from usable KVM access or an authorized physical device. Package validation still does not cover remote production endpoints, signing/notarization, store submission, physical/OEM behavior, or iOS WebView-specific network behavior.

## Previous verification checkpoint and current blockers

The following rows mix previous checkpoint validation with current blockers. Do not label the older aggregate counts as final evidence for the current completion gate; rerun the relevant gates when producing a release candidate or moving external platform validation from pending to supported.

| Gate | Result |
| --- | --- |
| Playwright installation | Playwright `1.61.1`; Chromium, Firefox, and WebKit executables installed. |
| Python quality/config | `make check` and `make check-config-generated` passed. |
| Python suite | Previous checkpoint `make unit`: **2195 passed, 38 skipped**, 76% coverage. Previous broader `make test` baseline: **2266 passed, 76 skipped**, 77% coverage; all **18** Python E2E tests, including both two-instance mesh modes, passed earlier in this change series. Rerun for final validation after remaining integration fixes. |
| TypeScript suite | Previous checkpoint `pnpm -r test`: **778 passed** across SDK (319), shared UI (280), Tauri (162), web (14), and mock (3). The Tauri aggregate runs test files serially so subprocess-heavy package validation tests cannot starve UI route-test timers; Android/iOS/desktop bundle validation files explicitly use the Node test environment. Rerun for final validation after the pending desktop live fix. |
| TypeScript static/build | `pnpm -r typecheck` and `pnpm -r build` passed. |
| Rust host checks | `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`, and `cargo test --all-targets --all-features` passed; **46 Rust tests passed**. |
| Browser route smoke | Playwright route crawl: **4 passed**. |
| Browser encrypted persistence | Real reload/restoration passed in Chromium, Firefox, and WebKit: **3 passed** with no plaintext reconnect/room secrets in browser storage. Maintained command: `pnpm test:web-persistence`; output is local generated diagnostics. |
| Live WebRTC | All **9** Chromium/Firefox/WebKit direct/STUN/TURN lanes passed with the Gateway HTTP API disabled and report redaction enabled. Hosted peer flow is maintained by `pnpm test:hosted-peer:live`; hosted mesh-node flow is maintained by `scripts/hosted_mesh_node_e2e.sh`; output is local generated diagnostics. |
| Desktop client package/live | Source and package gates are implemented. Packaged Python-free Linux desktop live E2E is maintained by `pnpm test:desktop-client:live`; packaged macOS/Windows live behavior remains external. |
| Android client package/static | `pnpm --filter @aurora/tauri-ui test:android-client-bundle` passed with 14 tests, `android:init` generated/synced the project, `android:preflight:ci` passed all 27 required generated-project/native-plugin checks, and APK/AAB artifact validation passed. Release signing remains non-required local evidence unless strict release preflight is selected. |
| Android emulator/device runtime | Blocked locally by host/device access: adb and Mobile MCP have no device, the x86_64 AVD cannot use `/dev/kvm`, ARM64 software emulation is unsupported on x86_64 QEMU2, and no Waydroid/binder route is available. Exact prerequisite: usable KVM access or an authorized physical device. |
| Android evidence integrity | Bundle validation unit tests write only to temporary config, provenance, proof, and preflight paths; release artifact evidence should be regenerated for a final release candidate. |
| Android preflight | At `2026-07-30T14:47:59.342Z`, **27 required checks passed**; the one non-required blocked item is absent release-signing configuration in local non-strict mode. |
| iOS client source/policy | `ios:policy`, `build:frontend:ios-client`, and `ios:prepare:client` pass for shared iOS WebView routing, Keychain/profile/proof source invariants, runtime-configurable overlay generation, and frontend build compatibility. Consolidated SDK/UI tests and builds also pass. This is not Xcode build, simulator, packaged WKWebView, Swift runtime, signing, or App Store validation. |
| iOS MobileSafari/WKWebView ↔ Python WebRTC | One normal serial E2E test, macOS workflow wiring, shared assertion/scanner reuse, separate per-surface generated outputs, packaged-app path scanning, and Linux skip/transform/typecheck gates are implemented. Passing MobileSafari and packaged-Tauri-WKWebView simulator reports remain pending and require macOS/Xcode with simulator/runtime/toolchain prerequisites; physical-device direct/STUN/TURN evidence remains separate. |

The local Android toolchain uses Temurin 17, Android platforms 35/36 where installed, and the Android Rust targets required by the client artifacts. Runtime emulator validation is blocked here by unavailable KVM/device access; CI or release validation must rerun the Android runtime gates with usable KVM access or an authorized physical device.

## Voice and native capability boundaries

Voice ownership remains surface-aware through `packages/aurora-ui/src/platform-surface.ts`:

- **Desktop local:** durable wakeword/background capture remains Python `STTCoordinator` ownership. Focused WebView push-to-talk/waveform can run while the UI is foregrounded.
- **Hosted web and desktop client:** focused WebView capture can support push-to-talk/read-aloud. They must not claim durable background listening.
- **Android client:** WebView microphone permission decisions require trusted HTTPS/`tauri.localhost` origin, Android `RECORD_AUDIO`, foreground, and focus. The package also declares the normal `MODIFY_AUDIO_SETTINGS` permission required for WebView/WebRTC audio routing. Runtime microphone and Python-peer evidence must be rerun with usable KVM access or an authorized physical device before release claims. Lifecycle status still sets `backgroundWakeword=false` and `mustReleaseMicrophone=true` when backgrounded/unfocused.
- **iOS:** the foreground client HTTP/WebRTC source path, native credential/profile bridge, frontend build, policy gate, and runtime-configurable overlay generation are implemented. Focused WebView microphone use remains subject to WKWebView/iOS permission and lifecycle proof on macOS/Xcode simulator or device; durable background voice remains unsupported. Siri/Shortcuts/App Intents/share/deep-link entrypoints remain app-owned and user-invoked.

Android native evidence covers policy/status/proof commands and build artifacts in this snapshot. It does not prove background service behavior, local model execution, a durable wakeword/audio pipeline, additional OEMs, or physical-device WebRTC direct/STUN/TURN.

## Python sidecar and Rust integration verdict

Desktop-local Python remains an **external supervised process**. Rust starts/stops the sidecar, probes loopback Gateway readiness, and exposes narrow command surfaces. Thin shells are Python-free because they use remote HTTP or the shared TypeScript WebRTC protocol; they do not compile Aurora's Python services into Rust. The Linux peer primitive is a Rust ICE/DTLS/SCTP implementation, not embedded Python and not a second Aurora Gateway.

Python-in-Rust alternatives were rejected/deferred for the current product split:

| Option | Verdict |
| --- | --- |
| Embed CPython/PyO3 in Rust | Rejected/deferred: still ships Python/runtime/native wheels and weakens the tested process isolation boundary. |
| Convert Aurora services to Cython/Nuitka libraries | Rejected/deferred: high compatibility risk and still Python-runtime coupled for much of the stack. |
| Rewrite Gateway/Aurora WebRTC protocol/services in Rust | Deferred: duplicates signaling, protocol, auth, mesh, and bus behavior and creates a long-term parity burden. |
| Narrow native Linux peer primitive | Implemented only as an injected `RTCPeerConnection`/DataChannel fallback when WebKitGTK omits the DOM API; TypeScript retains all Aurora protocol ownership. |
| Keep external Python sidecar for desktop-local plus Python-free client shells | Current direction: preserves local capability while letting WebView clients run without Python. |

## Rollback and runtime profile modes

Runtime rollback uses explicit profile selection rather than hidden transport mutation:

- Set the surface-specific `*_AURORA_WEBRTC_THIN_CLIENT` build gate to false for an emergency stop of new WebRTC sessions. Preferred profiles with a configured Gateway select HTTP; WebRTC-only profiles report disabled and fail closed.
- Switch hosted web or Tauri client profile to `http-only` to bypass WebRTC for new calls.
- Use `webrtc-only` only when the operator wants fail-closed direct-peer behavior with no HTTP fallback.
- Use `webrtc-preferred` when both a Gateway endpoint and WebRTC invite/profile exist; fallback applies to new calls only.
- Revoke a peer credential to force the next WebRTC route to fail closed and return to SAS pairing. Every current live lane report validates this behavior.
- Reconfigure the saved client profile when the operator Gateway or signaling endpoint changes; rebuilt artifacts are not required for ordinary endpoint changes.
- Keep desktop-local sidecar profiles (`desktop-local-minimal`, `local-cpu`, GPU profiles, `full`) separate from client profiles. Do not use sidecar preparation as a rollback for web/desktop/mobile clients.
- Do not delete, rewrite, or translate peer or HTTP credentials during rollback. Re-enable uses the same saved material unless it was separately revoked.

## Security limitations still open

- Direct, configured-STUN, and forced-TURN foreground interop is live-proven in Chromium, Firefox, and Playwright WebKit. Hosted Chromium product-flow E2E is live-proven against the real Python service. Packaged Linux desktop native-fallback WebView E2E is live-proven; packaged macOS/Windows live proof remains external platform evidence. Android Python-peer runtime evidence is blocked locally by unavailable KVM/device access and must run with usable KVM access or an authorized physical device. The iOS packaged-WKWebView simulator gate requires macOS/Xcode and lacks a local Linux result. Background/page-suspension behavior, broader physical/OEM paths, and production-scale browser certification remain unproven.
- TURN was validated against local coturn, not a production TURN service under load.
- Long-running reconnect, large messages, fragmentation under hostile networks, and production-scale MQTT/TURN observability require further soak/load tests.
- Hosted-web reconnect and room secrets are AES-GCM encrypted before IndexedDB storage; only validated nonsecret profile/stable-ID metadata is written to `localStorage`, and any persistence failure downgrades to memory-only. The non-extractable WebCrypto key currently lives in the same origin-scoped IndexedDB database, so this protects against plaintext inspection/accidental disclosure but **not** a compromised browser profile or active same-origin XSS that can invoke the application and WebCrypto. Desktop/mobile native stores hold only scoped peer reconnect material and must keep raw long-lived secrets out of logs, URLs, profiles, `localStorage`, and `sessionStorage`. Native proof commands must stay vector-compatible with canonical `mesh_auth_proof_v1` HMAC fixtures, including Python `ensure_ascii` behavior for Unicode values, and must not emit legacy `proof_hmac_sha256`.
- Stronger long-term hosted-web design: derive/unseal the vault key through a user-verified WebAuthn PRF/passkey and replace stored bearer reconnect with a server challenge signed by a WebAuthn-bound device key. That removes the reusable bearer from browser storage and makes offline profile theft materially harder; it still requires strict CSP/Trusted Types and dependency hygiene because no in-page vault survives active XSS after unlock.
- Scoped authorization remains on public production boundaries: SAS-bound Auth pairing/connect/exchange and public Gateway/Auth methods over the DataChannel, not private/test-only bypasses or internal service calls. Client peers should advertise consumer-only/minimal manifests and reject unsolicited inbound service calls unless explicitly configured.
- Remote audio/wakeword sharing remains policy-gated; no raw microphone or durable wakeword stream is release-ready without explicit user consent, selectors, and platform proof.
