PER-157: implement Tooling sharing policy and approval-token protocol for local and mesh tools on branch multica/PER-157-tool-sharing-approval. Scope: typed Tooling.GetSharingPolicy, SetSharingPolicy, TestSharingPolicy, PrepareExecution, RequestApproval, ConfirmExecution contracts; ToolingService discovery filtering and execution enforcement; approval_token required instead of raw confirmed=true for approval-required local and remote tools; audit via Auth.StoreAuditEvent; tests for deny_all, ask_each_time, allow_once, allow_until_expiry, approve_all_for_session, approve_all_for_peer, approve_all_local_safe, dry_run_only, raw confirmed bypass denial, token replay/expiry/mismatch, local safe approve-all and remote dangerous approval-required. PR target: feat/mesh-full-services-integrations.
