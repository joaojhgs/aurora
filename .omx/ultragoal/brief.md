Fix two Aurora issues in parallel while preserving concurrent frontend changes: (1) tooling/orchestrator/mesh regression where the LLM sees no tools, including core tools such as web search. Investigate tooling policy, tool sharing via mesh peers, catalog negotiation, and orchestrator exposure. Restore rule: only policy-blocked tools are hidden; insecure or not-yet-trusted tools remain visible and require permission before execution. Ensure core tools remain visible and add/update focused regressions. (2) desktop Tauri voice overlay polish: reduce transparent visible corner/background from the effect without reducing animation, allow dragging from any non-interactive overlay area, and show drag cursor only during active drag except the three-dot handle. Acceptance: targeted Python tooling/orchestrator tests pass; targeted Tauri/UI overlay tests and typechecks pass; no unrelated files reverted; small focused changes.
