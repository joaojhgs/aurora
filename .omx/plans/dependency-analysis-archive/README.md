# Archived dependency-analysis artifacts

These files were moved from `docs/dependency-analysis/` during documentation consolidation. They include investigation notes, generated dependency trees, security-audit snapshots, and phase/status journals.

Use `docs/DEPENDENCIES.md` for current dependency guidance. Regenerate dependency trees or audit output locally instead of committing generated report artifacts under `docs/`.
