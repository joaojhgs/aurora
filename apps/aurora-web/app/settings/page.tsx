import { AuroraRoutePage } from '../page-content'

export default function Page() {
  return (
    <AuroraRoutePage
      routeId="settings"
      title="Settings"
      description="Privacy defaults, permissions, native capability state, and config changes must stay backend-proven and AdminAction-gated."
    />
  )
}
