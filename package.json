{
  "name": "aurora-workspace",
  "private": true,
  "packageManager": "pnpm@10.25.0",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "test:webrtc:interop": "scripts/webrtc_interop_services.sh up && scripts/webrtc_interop.sh direct",
    "test:webrtc:turn": "scripts/webrtc_interop_services.sh up && scripts/webrtc_interop.sh turn",
    "test:webrtc:browsers": "scripts/webrtc_interop_browser_matrix.sh"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "typescript": "5.7.3",
    "vitest": "^4.0.16"
  }
}
